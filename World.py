world = {'x': 0,
         'y': 0,
         'width': 2500,
         'height': 2500}

start = {'x': 500,  # >= camera_size['width']/2
         'y': 500,  # >= camera_size['height']/2
         'hero_r': 35}

camera_size = {'width': 700,
               'height': 600}

enemies = 40  # count of enemies

HERO_MAX_SPEED = 8
DELAY = 20
