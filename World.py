world = {'x': 0,
         'y': 0,
         'width': 1500,
         'height': 1500}

spawn = {'x': 400,  # >= camera_size['width']/2
         'y': 400}  # >= camera_size['height']/2

camera_size = {'width': 500,
               'height': 500}

enemies = 10  # count of enemies

hero_speed = 20
