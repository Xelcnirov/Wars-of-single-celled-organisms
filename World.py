world = {'x': 0,
         'y': 0,
         'width': 1500,
         'height': 1500}

start = {'x': 500,  # >= camera_size['width']/2
         'y': 500,
         'hero_r': 40}  # >= camera_size['height']/2

camera_size = {'width': 500,
               'height': 500}

enemies = 30  # count of enemies

hero_speed = 10
